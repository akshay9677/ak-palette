import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faRocket } from "@fortawesome/free-solid-svg-icons";
import { Button } from "../../src/index";

Button Component is use to render a button in the user interface, that can perform tasks when clicked

### Appearance

The Palette Button component has two `appearance`

- `primary`
- `secondary`
- `tertiary`
- `link`

##### Example

<div
  style={{
    display: "flex",
    flexWrap: "wrap",
  }}
>
  <span style={{ padding: "10px" }}>
    <Button onClick={() => alert("hello")}>Primary Button</Button>
  </span>
  <span style={{ padding: "10px" }}>
    <Button appearance="secondary" onClick={() => alert("hello")}>
      Secondary Button
    </Button>
  </span>
  <span style={{ padding: "10px" }}>
    <Button appearance="tertiary" onClick={() => alert("hello")}>
      Tertiary Button
    </Button>
  </span>
  <span style={{ padding: "10px" }}>
    <Button
      appearance="link"
      onClick={() => window.open("https://www.google.com/")}
    >
      Link Button
    </Button>
  </span>
</div>

#### Code

```jsx
<Button onClick={() => alert("hello")}>Primary Button</Button>
<Button appearance="secondary" onClick={() => alert("hello")}>
  Secondary Button
</Button>
<Button appearance="tertiary" onClick={() => alert("hello")}>
  Tertiary Button
</Button>
<Button
  appearance="link"
  onClick={() => window.open("https://www.google.com/")}
>
  Link Button
</Button>
```

### Size

Since the Button component renders the children as it is, the height is determined by the children.

<Button>
  <div
    style={{
      height: "50px",
      display: "flex",
      alignItems: "center",
      fontSize: "16px",
    }}
  >
    Large Button
  </div>
</Button>

#### Code

```jsx
<Button>
  <div
    style={{
      height: "50px",
      display: "flex",
      alignItems: "center",
      fontSize: "16px",
    }}
  >
    Large Button
  </div>
</Button>
```

### Color

The Palette Button component has `type` prop which is used as the background color or the text color depending on the `appearance`
prop.

There are five color types,

- `default`
- `success`
- `info`
- `warning`
- `danger`

The following is the way in which the type is applied

> - `appearance` - `primary` then `type` - `background-color`
> - `appearance` - `secondary` then `type` - `text color`
> - `appearance` - `tertiary` then `type` - `text color`
> - `appearance` - `link` then does not apply

##### Primary Appearance

<div>
  <div
    style={{
      display: "flex",
      flexWrap: "wrap",
    }}
  >
    <span style={{ padding: "6px" }}>
      <Button type="success">Success</Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="info">Info</Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="warning">Warning</Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="danger">Danger</Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="default">Default</Button>
    </span>
  </div>
</div>

##### Secondary Appearance

<div>
  <div
    style={{
      display: "flex",
      flexWrap: "wrap",
    }}
  >
    <span style={{ padding: "6px" }}>
      <Button type="success" appearance="secondary">
        Success
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="info" appearance="secondary">
        Info
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="warning" appearance="secondary">
        Warning
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="danger" appearance="secondary">
        Danger
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="default" appearance="secondary">
        Default
      </Button>
    </span>
  </div>
</div>

##### Tertiary Appearance

<div>
  <div
    style={{
      display: "flex",
      flexWrap: "wrap",
    }}
  >
    <span style={{ padding: "6px" }}>
      <Button type="success" appearance="tertiary">
        Success
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="info" appearance="tertiary">
        Info
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="warning" appearance="tertiary">
        Warning
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="danger" appearance="tertiary">
        Danger
      </Button>
    </span>
    <span style={{ padding: "6px" }}>
      <Button type="default" appearance="tertiary">
        Default
      </Button>
    </span>
  </div>
</div>

#### Code

```jsx
<Button type="success">Success</Button>
<Button type="info">Info</Button>
<Button type="warning">Warning</Button>
<Button type="danger">Danger</Button>
<Button type="default">Default</Button>
```

> The above code is only for appearance `"primary"`, the same applies to other two appearance - `"secondary"` & `"tertiary"`

### Icon buttons

Since the child of the Button component is rendered as it is when can render icons easily

<div style={{ display: "flex", flexDirection: "row" }}>
  <div style={{ display: "flex", padding: "10px" }}>
    <Button>
      <span style={{ padding: "0px 10px 0px 0px", fontSize: "13px" }}>
        <FontAwesomeIcon icon={faRocket} />
      </span>
      Send
    </Button>
  </div>
  <div style={{ display: "flex", padding: "10px" }}>
    <Button appearance="tertiary">
      <span style={{ fontSize: "13px" }}>
        <FontAwesomeIcon icon={faRocket} />
      </span>
    </Button>
  </div>
</div>

#### Code

```jsx
<Button>
  <FontAwesomeIcon icon={faRocket} />
  Send
</Button>
```

### Disabled button

The `disabled` prop is used to render a disabled button in the user interface

<div style={{ display: "flex", paddingBottom: "25px", paddingTop: "10px" }}>
  <span style={{ paddingRight: "10px" }}>
    <Button disabled>Disabled Button</Button>
  </span>
  <Button disabled appearance="secondary">
    Disabled Button
  </Button>
</div>

#### Code

```jsx
<Button disabled>Disabled Button</Button>
<Button disabled appearance="secondary">Disabled Button</Button>
```

### API

|    Prop    |    Type    |                        Possible values                         | Default Value |
| :--------: | :--------: | :------------------------------------------------------------: | :-----------: |
| appearance |  `string`  |      `"primary"`,`"secondary"`,`"tertiary"` and `"link"`       |  `"primary"`  |
|    type    |  `string`  | `"default"`, `"success"`, `"info"`, `"danger"` and `"warning"` |   `default`   |
|  disabled  | `boolean`  |                       `true` and `false`                       |    `false`    |
|  onClick   | `()=>void` |                               -                                |       -       |

Built by Akshay 🤙

import { Meta, Story } from "@storybook/addon-docs/blocks";
import CheckBox from "./Checkbox.tsx";

<Meta title="Palette Checkbox" />

# Palette Checkbox

Palette Component is use to render a checkbox input in the user interface.

## Basic Usage

<Story name="Checkbox">
  <div style={{ display: "flex", paddingBottom: "25px", paddingTop: "10px" }}>
    <CheckBox
      label="Checkbox"
      onChange={(e) => console.log(`The value is ${e}`)}
    />
  </div>
</Story>

#### Code

```jsx
// default checkbox
 let React from 'react'

 const MyComponent = () => {
  const [checked, setChecked] = React.useState(false);
  return (
    <Checkbox
      label="Checkbox"
      checked={checked}
      onChange={(e) => {
        setChecked(e);
      }}
    />
  );
};
```

## Default checked

The starting value for a check box can also be mentioned in Palette Checkbox by providing a boolean value to the checked prop

<Story name="Default value">
  <div style={{ display: "flex", paddingBottom: "25px", paddingTop: "10px" }}>
    <CheckBox
      label="Checkbox"
      checked={true}
      onChange={(e) => console.log(`The value is ${e}`)}
    />
  </div>
</Story>

#### Code

```jsx
// default checkbox
 let React from 'react'

 const MyComponent = () => {
  const [checked, setChecked] = React.useState(true);
  return (
    <Checkbox
      label="Checkbox"
      checked={checked}
      onChange={(e) => {
        setChecked(e);
      }}
    />
  );
};
```

## Disabled Checkbox

<Story name="Disabled">
  <div style={{ display: "flex", paddingBottom: "25px", paddingTop: "10px" }}>
    <CheckBox
      label="Checkbox"
      onChange={(e) => console.log(`The value is ${e}`)}
      disabled
    />
  </div>
</Story>

#### Code

```jsx
// default checkbox
<CheckBox label="Checkbox" disabled />
```

## Properties

|   Prop   |    Type    |  Possible values   | Default Value |
| :------: | :--------: | :----------------: | :-----------: |
|  label   |  `string`  |  Any String value  |     `""`      |
| checked  | `boolean`  | `true` and `false` |    `false`    |
| disabled | `boolean`  | `true` and `false` |    `false`    |
| onChange | `function` |         --         |      --       |

Built by Akshay 🤙
